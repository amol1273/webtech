PERSON.RB CODE:

class Person
  # Initialize the class with name, age, and email attributes
  def initialize(name, age, email)
  @name = name
  @age = age
  @contact = email
  end

  # Getter for the name
  def name
  @name
  end
  # Setter for the name
  def name=(name)
  @name = name
  end

  # Getter for the age
  def age
  @age
  end
  # Setter for the age
  def age=(age)
  @age = age
  end

  # Getter for the email
  def email
  @contact
  end
  # Setter for the email
  def email=(email)
  @contact = email
  end
  
  end

TEST_PERSON.RB CODE:

require_relative 'person'

person = Person.new('vaibhav', 30, 'vaibhav@gmail.com')


puts "Original Name: #{person.name}"
puts "Original Age: #{person.age}"
puts "Original Email: #{person.email}"


person.name = 'Amol'
person.age = 21
person.email = 'amol@gmail.com'


puts "Updated Name: #{person.name}"
puts "Updated Age: #{person.age}"
puts "Updated Email: #{person.email}"
